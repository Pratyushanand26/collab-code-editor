export interface User { id: string; name: string; color: string; }
export interface Room { id: string; language: string; }
export interface ExecutionResult { stdout: string; stderr: string; exitCode: number; }
